import type { NextConfig } from "next";

const nextConfig: NextConfig = {
	/* config options here */
	images: {
		domains: [
			"quanskill.kuditrak.ng",
			"api.quanskill.com",
			"api.quanskill.com/storage/lecturer_pictures/",
		],
	},
};

export default nextConfig;
